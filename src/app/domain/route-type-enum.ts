import { ɵCompiler_compileModuleAsync__POST_R3__ } from '@angular/core';

export enum RouteTypeEnum{
    UNIDIRECTIONAL = 'UNIDIRECTIONAL',
    BIDIRECTIONAL = 'BIDIRECTIONAL',
    CIRCULAR = 'CIRCULAR',
}
